{
  "name": "beacon-telemetry",
  "version": "1.0.0",
  "description": "Telemetry endpoint for the beacon Claude Code plugin",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:migrate": "prisma migrate deploy",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "fastify": "^5.3.0"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
